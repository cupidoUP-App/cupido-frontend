{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    //"allowImportingTsExtensions": true,
    //con bundler (Vite, esbuild), no se necesita escribir la extensión.
    //Activarlo puede generar inconsistencias si mezclas imports con y sin .ts.
    "isolatedModules": true,
    //"moduleDetection": "force",
    //"force" puede ser útil en casos raros (scripts sueltos),
    // pero en el repo React/Vite no aporta nada.
    "noEmit": true,

    /* Linting */
    "strict": true,
    //"noUnusedLocals": false,
    //"noUnusedParameters": false,
    //"noUnusedLocals" ni "noUnusedParameters" son necesarios. 
    // Mejor dejar que el compilador avise de basura en config scripts.
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts"]
}
